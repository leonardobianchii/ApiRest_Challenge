# ===== Configuracao principal (Azure SQL) =====
spring:
  datasource:
    url: jdbc:sqlserver://srv-mottu-sql.database.windows.net:1433;database=db-mottu;encrypt=true;trustServerCertificate=false;hostNameInCertificate=*.database.windows.net;loginTimeout=30;
    username: rm558576
    password: Fiap@Devops2025
    driver-class-name: com.microsoft.sqlserver.jdbc.SQLServerDriver

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    database-platform: org.hibernate.dialect.SQLServerDialect
    properties:
      hibernate.format_sql: true

  sql:
    init:
      mode: never

# H2/console removidos

# Flyway desativado
flyway:
  enabled: false
